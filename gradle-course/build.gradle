plugins{
    id 'java'
}

repositories {
    // use this repo for dependencies
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter:5.6.3'
}

test {
    useJUnitPlatform()
}

jar {
    manifest {
        attributes('Main-Class': 'com.rus.languageapp.SayHello')
    }
}